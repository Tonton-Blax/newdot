import { getContext, onMount } from 'svelte';
export function onTick(fn) {
    const app = getContext('pixi/app');
    onMount(() => {
        app.ticker.add(fn);
        return () => {
            app.ticker.remove(fn);
        };
    });
}
//# sourceMappingURL=onTick.js.map