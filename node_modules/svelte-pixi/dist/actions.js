import { setContext } from 'svelte';
export function mountPixi(node, app) {
    setContext('pixi/app', app);
    setContext('pixi/stage', app.stage);
    node.appendChild(app.view);
}
//# sourceMappingURL=actions.js.map